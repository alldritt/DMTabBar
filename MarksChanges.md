#Mark's Changes to DMTabBar

I have split the bar background drawing code into a separate class (DMBar) so that I can reuse that code and ensure visual consistency with other bar views in my application.